# Brian 管理說明（重要，請保存）

這份文件給你自己看，學員不會看到。

---

## 目前設定

- **通行碼**：`ceobrian`
- **驗證有效期**：365 天（一年）
- **搜尋引擎**：已封鎖（noindex + robots.txt）

---

## 怎麼換通行碼

打開 `js/auth.js`，找到最上面這兩行：

```javascript
const PASSWORD = 'ceobrian';       // ← 改這裡換密碼
const VALID_DAYS = 365;            // ← 一次驗證有效幾天
```

改完上傳到 GitHub，所有學員下次驗證就要用新密碼。

**舊密碼會自動失效**——舊驗證紀錄裡記的是舊密碼的 hash，密碼一改不符合，自動清掉。

---

## 換密碼的時機建議

你目前設定一年換一次，這是最寬鬆的做法。優點是學員體驗順、你不用一直管。

**何時該臨時換密碼**：
- 發現密碼被外流（FB 社團、Dcard 看到有人在分享）
- 課程改版、想清掉一批舊學員
- 你停止經營這個工具一段時間，重啟時重新開放

換完後在 LINE 通知學員「新密碼是 XXX」就好。

---

## 如果有人破解密碼怎麼辦

純前端的工具沒有 100% 防破解。技術夠強的人打開瀏覽器開發者工具就能看到密碼字串。

**但別擔心**，因為：

1. **學員都自己付 API Key 費用** — 就算被白嫖工具，他們也要燒自己的錢，沒什麼好損失的
2. **真的被人傳出去** — 你下次換密碼就斷了
3. **密碼是混淆過的** — 不是 `password = "ceobrian"` 這種一眼看穿，是 hash 後的數字，需要懂程式碼的人才能挖出來

如果你發現有人惡意散布密碼，**改密碼就好，5 秒鐘的事**。

---

## 搜尋引擎封鎖機制

已經做了三層防護：

1. **HTML meta tag**：每個頁面都標註 `noindex, nofollow`，告訴搜尋引擎「別收錄我」
2. **robots.txt**：放在網站根目錄，阻擋所有主流爬蟲（Google、Bing、百度、ChatGPT、Claude、Perplexity 等 AI 爬蟲）
3. **GitHub Pages 預設**：本來就比較難被搜到

**注意**：`robots.txt` 是「請求」不是「強制」。守規矩的爬蟲會遵守（Google、Bing 都會），但少數惡意爬蟲可能不會。即便如此，noindex meta 是雙保險，正派搜尋引擎絕對不會收錄。

**如果你還是擔心**：
- 不要在任何公開地方（FB 貼文、官網、Email）放出網址
- 只在 LINE 一對一傳給學員
- 如果發現被收錄了，可以在 [Google Search Console](https://search.google.com/search-console) 提交移除請求

---

## 怎麼追蹤誰在用工具

純前端的限制：你看不到使用紀錄，因為沒後端。

如果你想知道：
- 誰用了工具 → 升級到 C 版（前端 + Cloudflare Workers 後端，可記 log）
- 學員產出什麼地圖 → 同上，可存資料庫
- 哪個工具最受歡迎 → 同上，可加分析

升級的時機建議：**學員數超過 50 人之後再考慮**。在那之前 B 版完全夠用。

---

## 未來加新工具的步驟

1. 把新工具的 HTML 放到 `tools/` 資料夾，例如 `tools/quote.html`
2. 在新工具的 `<head>` 裡複製這段守門程式碼：
   ```html
   <meta name="robots" content="noindex, nofollow, noarchive, nosnippet, noimageindex">
   <meta name="googlebot" content="noindex, nofollow">
   <meta name="bingbot" content="noindex, nofollow">
   <script src="../js/auth.js"></script>
   <script>
     if (!window.BrianAuth.isVerified()) {
       window.location.href = '../index.html';
     }
   </script>
   ```
3. 在 `index.html` 找到 `<!-- 預留位 1 -->` 那塊，把 `class="tool-card coming"` 改成 `class="tool-card" data-tool="quote" data-href="tools/quote.html"`，並把 badge 從 `soon` 改成 `new`
4. 上傳到 GitHub

新工具自動繼承同一套通行碼驗證，學員不用重新驗證。

---

## 檔案結構說明

```
collab-explorer/
├── index.html              ← 首頁（選單 + 密碼驗證入口）
├── robots.txt              ← 阻擋搜尋引擎爬蟲
├── tools/
│   └── collab.html         ← 異業合作探索器
├── js/
│   └── auth.js             ← ⭐ 換密碼改這裡
├── README.md               ← 給學員看
└── 管理說明.md             ← 給你看（這份）
```

---

## 上 GitHub 注意事項

⚠️ **不要把密碼貼到 commit message 或 README 裡**。
密碼只能在 `js/auth.js` 裡。雖然 GitHub Public repo 任何人都看得到 auth.js，但密碼藏在程式碼裡不會被 Google 直接搜到。

如果你要更安全，把整個 repo 改成 **Private**——但要 GitHub Pro 帳號才能用 GitHub Pages，每月 $4 美金。
