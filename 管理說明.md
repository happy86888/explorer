# Brian's Toolkit · 管理說明

只有你（Brian）會看這份，學員看 README.md。

## 目前設定

- **通行碼**：`ceobrian`
- **驗證有效期**：100 年（學員這台電腦輸入一次後永久不用再輸）
- **搜尋引擎**：已封鎖（noindex + robots.txt）
- **可用工具**：10 個（00-09）

## 換通行碼

打開 `js/auth.js`，改第一行的 `PASSWORD`：

```js
const PASSWORD = 'ceobrian';  // ← 改這裡
```

改完上傳到 GitHub，學員下次進入需要重新輸入新密碼。

## 檔案結構

```
collab-explorer/
├── index.html              ← 首頁（10 個工具卡 + 密碼驗證）
├── robots.txt              ← 阻擋搜尋引擎
├── README.md               ← 給學員看
├── 管理說明.md             ← 給你看（這份）
├── css/
│   └── tools.css           ← 工具共用樣式
├── js/
│   ├── auth.js             ← ⭐ 改密碼在這裡
│   └── tools-common.js     ← 工具共用函式
└── tools/
    ├── newbie.html         ← 00 新手指南
    ├── collab.html         ← 01 異業合作
    ├── quote.html          ← 02 報價產生器
    ├── objection.html      ← 03 異議拆解
    ├── contract.html       ← 04 合約檢查
    ├── brief.html          ← 05 案件健診
    ├── translate.html      ← 06 溝通翻譯
    ├── portfolio.html      ← 07 作品集診斷
    ├── collect.html        ← 08 尾款催收
    └── retainer.html       ← 09 月顧問費
```

## 上 GitHub Pages 步驟

1. 到 GitHub 建一個新的 Public repo（不要勾 Add README）
2. Upload files → 拖入解壓縮後資料夾**裡面的所有東西**（不是資料夾本身）
3. Commit
4. Settings → Pages → Branch: main / root → Save
5. 等 1-2 分鐘，網址會在 Pages 頁面看到

## 如果要更新

最簡單的方式是「砍掉重建」：
- GitHub repo → Settings → Delete this repository
- 重新建同名 repo
- Upload 新版本

或者只更新有改的檔案（透過 GitHub web 介面拖拉上傳會覆蓋）。

## 學員費用

- 通行碼免費（你給）
- AI 費用學員自己付：每次產出約 NT$1-3
- 5 美金（約 NT$160）可用幾百次

學員在 [console.anthropic.com](https://console.anthropic.com) 申請 API Key，貼到工具裡（key 只存在學員瀏覽器，不會傳到別處）。

## 合規與責任

- 工具裡所有產出都標明「AI 是助手不是專業顧問」
- 合約、尾款催收等工具有明顯法律免責聲明
- 不會輸出個資、私人聯絡方式、違法資訊
- 學員私自濫用的責任不在你

## 維護建議

- 每隔 3-6 個月看一次：學員回饋哪個工具最常用、哪個不常用
- 不常用的可以下架；常用的可以加深功能
- 接案網清單（newbie.html）可能要每年更新一次（平台會關閉）

## 常見問題

**Q: 學員說 AI 沒回應？**
A: 通常是 API Key 過期或額度用完，請學員到 console.anthropic.com 檢查 Billing。

**Q: 學員說密碼被別人傳出去？**
A: 改 auth.js 的 PASSWORD，重新上傳即可。

**Q: 想新增工具？**
A: 複製一個現有的工具（例如 quote.html）改 prompt 跟介面，再到 index.html 加一張卡片。

**Q: 想關掉某個工具？**
A: 把 index.html 該卡片的 `<a>` 改成 `<div class="tool-card coming">`，學員就不能點了。
